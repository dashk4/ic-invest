# Laravel CMS this site reads all its content from (news, funds, documents,
# contact/CV forms, uploaded images). See src/lib/config.ts.
# Leave unset to use the current default: https://ic-invest.mn
# Set this once the CMS is served from its own subdomain:
NEXT_PUBLIC_CMS_BASE=https://cms.ic-invest.mn
